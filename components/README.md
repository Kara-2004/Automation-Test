# Shared components

`DataTable`, `ConfirmDialog` và `ToastMessage` được tách từ cấu trúc DOM đã quan sát tại trang Loại bản đồ. Các module danh mục tiếp theo có thể tái sử dụng chúng sau khi locator trang tương ứng được xác minh.
