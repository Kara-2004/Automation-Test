# Page Objects

`HomePage`, `LoginPage`, `AccountMenu` và `MapTypePage` chỉ chứa URL, accessible name, placeholder hoặc thuộc tính ARIA đã được quan sát trực tiếp trên môi trường DDBD. Không dùng XPath tuyệt đối, `nth-child` hoặc class CSS sinh ngẫu nhiên.
